name: First Workflow

on: workflow_dispatch

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello World!"

      - name: Print goodbye # Fixed indentation here and added a space after the hyphen
        run: echo "Goodbye"
